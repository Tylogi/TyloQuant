#pragma once

// Temporary source-compatibility include. New code should include
// <mfq/server.h>; this forwarding header can be removed after downstream
// consumers have migrated.
#include "mfq/server.h"
