# DeepSeek V4 compatibility surface

This directory contains only the stable V4 include façade. The raw-HF
execution core shared with V4.1 lives in `deepseek_family/`; reusable Metal
operators live in `backends/metal/ops/`. V4.1 implementation and tuning policy
must not be added here.
